Git: Look at the diff between the local directory, and HEAD, including all untracked files. Suggest a one-liner commit message. Do not actually commit yet. Also, print out `git diff --stat` for me to help decide.

When asked to commit, do add all files, including the untracked ones.

Run the `git push` command outside the sandbox.