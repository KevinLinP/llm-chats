Git: Look at the diff between the local directory, and HEAD, including untracked files. Suggest a one-liner commit message. Do not actually commit yet.

When asked to commit, do add all files, including the untracked ones.

Run the `git push` command outside the sandbox.